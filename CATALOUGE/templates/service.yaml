#Service 

apiVersion: v1
kind: Service
metadata:
  name: "{{.Values.service.name}}"
  namespace: "{{.Values.deployment.namespace}}"
  labels:
    project: roboshop
    environment: dev
    purpose: poc

spec:
  type: NodePort 
  selector:
    app: "{{ .Values.deployment.deployment-name }}"
    project: roboshop
    environment: dev
    purpose: poc 
  
  ports:
  - protocol: TCP
    port: 27017  #service port
    targetPort: 27017 #container port
    nodePort: 30017